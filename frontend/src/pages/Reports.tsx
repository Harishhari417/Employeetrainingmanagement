import { useEffect, useState } from "react";
import { BarChart3, Download, FileSpreadsheet, Users } from "lucide-react";
import { api } from "../services/api";
import { useAuth } from "../auth/AuthContext";
type Training = { _id: string; title: string; trainingDate?: string };
async function downloadReport(path: string, filename: string) { const r = await api.get(path, { responseType: 'blob' }); const url = URL.createObjectURL(r.data); const a = document.createElement('a'); a.href = url; a.download = filename; document.body.appendChild(a); a.click(); a.remove(); URL.revokeObjectURL(url) }
export default function Reports() { const { user } = useAuth(); const [trainings, setTrainings] = useState<Training[]>([]); const [trainingId, setTrainingId] = useState(''); const [startDate, setStartDate] = useState(''); const [endDate, setEndDate] = useState(''); useEffect(() => { api.get<Training[]>('/trainings').then(r => setTrainings(r.data || [])).catch(() => { }) }, []); const query = () => { const q = new URLSearchParams(); if (trainingId) q.set('trainingId', trainingId); if (startDate) q.set('startDate', startDate); if (endDate) q.set('endDate', endDate); return q.toString() }; return <div className="space-y-6"><div><p className="text-sm font-medium text-indigo-600">Reports</p><h1 className="mt-1 text-2xl font-bold sm:text-3xl">Training Reports</h1><p className="mt-2 text-sm text-slate-500">Select a particular training or date range before exporting.</p></div><section className="rounded-2xl border bg-white p-5 shadow-sm"><div className="grid gap-3 md:grid-cols-4"><select value={trainingId} onChange={e => setTrainingId(e.target.value)} className="rounded-xl border px-3 py-3 text-sm md:col-span-2"><option value="">All trainings</option>{trainings.map(t => <option key={t._id} value={t._id}>{t.title}</option>)}</select><input type="date" value={startDate} onChange={e => setStartDate(e.target.value)} className="rounded-xl border px-3 py-3 text-sm" /><input type="date" value={endDate} onChange={e => setEndDate(e.target.value)} className="rounded-xl border px-3 py-3 text-sm" /></div></section><div className="grid gap-5 md:grid-cols-2"><section className="rounded-2xl border bg-white p-6 shadow-sm"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-indigo-50 text-indigo-600"><BarChart3 size={22} /></div><h2 className="mt-5 text-lg font-bold">Training report</h2><p className="mt-2 text-sm text-slate-500">Training, trainer, date, participants, attendance, feedback and evaluations.</p><button onClick={() => downloadReport(`/reports/training${query() ? `?${query()}` : ''}`, 'training-report.csv')} className="mt-5 inline-flex items-center gap-2 rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white"><Download size={17} /> Export CSV</button></section>{user?.role === 'HR_ADMIN' && <section className="rounded-2xl border bg-white p-6 shadow-sm"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-emerald-50 text-emerald-600"><Users size={22} /></div><h2 className="mt-5 text-lg font-bold">Employee training report</h2><p className="mt-2 text-sm text-slate-500">Employee, department, manager, assignments and attendance percentage.</p><button onClick={() => downloadReport('/reports/employees', 'employee-training-report.csv')} className="mt-5 inline-flex items-center gap-2 rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white"><FileSpreadsheet size={17} /> Export CSV</button></section>}</div></div> }
