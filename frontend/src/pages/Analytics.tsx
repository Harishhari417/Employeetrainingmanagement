import { useEffect, useState } from "react";
import { BarChart3, ClipboardCheck, MessageSquareText, TrendingUp } from "lucide-react";
import { api } from "../services/api";

type Data={averageFeedback:number;attendancePercentage:number;feedbackCount:number;byTraining:{name:string;attendance:number;feedback:number}[]};
export default function Analytics(){const [data,setData]=useState<Data>({averageFeedback:0,attendancePercentage:0,feedbackCount:0,byTraining:[]});useEffect(()=>{api.get<Data>("/analytics").then(r=>setData(r.data)).catch(()=>{});},[]);return <div className="space-y-6"><div><p className="text-sm font-medium text-indigo-600">Reports & Analytics</p><h1 className="mt-1 text-2xl font-bold sm:text-3xl">Training Analytics</h1><p className="mt-2 text-sm text-slate-500">Monitor attendance, feedback and training outcomes.</p></div><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><ClipboardCheck className="text-indigo-600" size={22}/><p className="mt-4 text-xs uppercase tracking-wide text-slate-500">Attendance</p><p className="mt-1 text-3xl font-bold">{data.attendancePercentage}%</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><MessageSquareText className="text-indigo-600" size={22}/><p className="mt-4 text-xs uppercase tracking-wide text-slate-500">Average feedback</p><p className="mt-1 text-3xl font-bold">{data.averageFeedback} / 5</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><TrendingUp className="text-indigo-600" size={22}/><p className="mt-4 text-xs uppercase tracking-wide text-slate-500">Feedback responses</p><p className="mt-1 text-3xl font-bold">{data.feedbackCount}</p></div></div><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><BarChart3 size={20} className="text-indigo-600"/><div><h2 className="font-semibold">Training performance</h2><p className="text-sm text-slate-500">Attendance and feedback by programme</p></div></div><div className="mt-6 space-y-5">{data.byTraining.length?data.byTraining.map(x=><div key={x.name}><div className="flex justify-between gap-3 text-sm"><span className="font-medium">{x.name}</span><span className="text-slate-500">{x.attendance}% attendance · {x.feedback}/5 feedback</span></div><div className="mt-2 h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-indigo-600" style={{width:`${Math.min(100,x.attendance)}%`}}/></div></div>):<p className="text-sm text-slate-500">No training analytics available yet.</p>}</div></section></div>}
