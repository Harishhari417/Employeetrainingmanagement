import { useEffect, useState } from "react";
import { Plus } from "lucide-react";
import { api } from "../services/api";

type Department={_id:string;name:string;status:string};
export default function Departments(){ const [rows,setRows]=useState<Department[]>([]); const [name,setName]=useState(""); const [error,setError]=useState(""); const load=async()=>{try{setRows((await api.get("/departments")).data)}catch(err:any){setError(err?.response?.data?.message||"Unable to load departments.")}}; useEffect(()=>{load()},[]); const add=async()=>{if(!name.trim())return;setError("");try{await api.post("/departments",{name:name.trim()});setName("");load()}catch(err:any){setError(err?.response?.data?.message||"Unable to add department.")}}; return <div className="space-y-6"><div><p className="text-sm font-medium text-indigo-600">Settings</p><h1 className="mt-1 text-2xl font-bold sm:text-3xl">Department Management</h1><p className="mt-2 text-sm text-slate-500">Manage departments used by employees and training assignments.</p></div>{error&&<div className="rounded-xl border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-700">{error}</div>}<div className="flex flex-col gap-3 sm:flex-row"><input value={name} onChange={e=>setName(e.target.value)} onKeyDown={e=>e.key==="Enter"&&add()} placeholder="New department name" className="flex-1 rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm"/><button onClick={add} className="inline-flex items-center justify-center gap-2 rounded-xl bg-indigo-600 px-5 py-3 text-sm font-semibold text-white"><Plus size={18}/> Add Department</button></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{rows.map(d=><div key={d._id} className="flex items-center justify-between rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div><p className="font-semibold">{d.name}</p><p className="mt-1 text-xs text-slate-500">{d.status}</p></div></div>)}</div></div> }
